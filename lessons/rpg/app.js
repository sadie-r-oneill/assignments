const readLine = require ("readLine-sync")

console.log (readLine)

const playerName = readline.question ("what is your name?")
const enemy = 
console.log ("thank you" + playerName)

// Global Variables

const userSelections = ["walk"]
let gameover = false

//game loop

while(!gameover){

const userChoice = readLine.keyInSelect(userSelections, "what would you like to do?")
console.log (userChoice)

}

if (userChoice ===0){

    console.log ("You walked")
}

if (userChoice ===1){

    console.log ("you cannot cancel")
}

//math.floor (5.67385)
//console.log (math.floor (math.random()) *3 )

function generateRandomChance(){
return Math.floor(math.random() * chance)
}

const player = {
    name: ""
    health: 100
    item: []
}

// \n = new line 
// \t = tab over elements in console

// ProcessingInstruction.exit() = quit a node 
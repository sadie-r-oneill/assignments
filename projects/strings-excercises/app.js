//Make a function that will return any given string into 
//all caps followed by the same string all lowercase

    // var word = "hello"
    // var upperCasedHello = word.toUpperCase()
    // var lowerCasedHello = word.toLowerCase()
    // console.log (upperCasedHello)
    // console.log (lowerCasedHello)

//Make a function that returns a number half the length, 
//and rounded down. You'll need to use Math.floor().

var word = "hello"
var indexOfWord = word.indexOf("l")
console.log(indexOfWord)
Math.floor("2")
console.log("2")

var phrase = "hello world"
var indexOfPhrase = phrase.indexOf("5")
console.log("5")

// Make a function that uses slice() and the other functions you've
// written to return the first half of the string

// var hello = "hello world"

// var firstHalf = hello.slice(0,6)
// console.log(firstHalf)

var hello = "hello"
hello.toUpperCase()

var world = " WORLD"
world.toLowerCase()

var both = hello.concat(world)
console.log(both)




